## Process this file with automake to produce Makefile.in

# subdirectories to scan
SUBDIRS = build glade help icons pixmaps po src

if USE_GROFF
  SUBDIRS += man
endif

EXTRA_DIST = config.rpath gfm.pro INFO

